# Research and Architecture

ForgeGUI keeps third-party crates behind ForgeGUI-owned adapters. Current pinned evaluation candidates include egui_extras, egui_ltreeview, egui_tiles, egui_kittest, and egui-phosphor. Saved project formats must not serialize their private types.
